# set up node modules `npm install` (developed using node 10.x)
# run the application `npm run start`
# see endpoing `http://localhost:8080/hello`
# see long-running computation endpoint `http://localhost:8080/naptime`