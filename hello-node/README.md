# set up node modules `npm install` (developed using node 24.18.1 LTS, installed via `brew install node@24`)
# run the application `npm run start`
# see endpoint `http://localhost:8080/hello`
# see long-running computation endpoint `http://localhost:8080/naptime`
